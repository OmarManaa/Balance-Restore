# Balance & Restore v4

Complete replacement project with working navigation, editable typography, consistent About headings, social SVG icons, modern feminine design, theme presets, safe D1 merging and promotion dates.

Keep your existing working `wrangler.jsonc`. Replace `public/`, `src/`, `migrations/`, `package.json`, and `.gitignore`.

Test:

```powershell
npm install
npm run dev
```

Deploy:

```powershell
git add .
git commit -m "Upgrade Balance Restore to v4"
git push
```
