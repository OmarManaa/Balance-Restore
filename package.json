{
  "name": "balance-restore-clean-v6",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply balance-restore-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply balance-restore-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}